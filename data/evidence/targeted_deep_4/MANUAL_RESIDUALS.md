# Manual residuals after targeted-deep run 4

No further general-purpose Actions harvesting is recommended.

1. **Farlow / Cheever B21-B25** — these five box pages were linked by the public index but were not harvested in run 4. Check manually only if a complete Cheever position index is useful. Several harvested boxes are also incomplete in the public table (`B17=99`, `B26=29`, `B28=99`, `B34=77`, `B36=58`); `B16` has 101 source rows because position `B16/97` occurs twice with conflicting table rows. Preserve the collision rather than choosing one silently.
2. **Harvard MCZ** — three targeted MCZbase requests timed out. Open the relevant named collection/object pages manually only when a concrete research use requires them.
3. **ANSP Symbiota** — do not treat the `Smith pre-1900` / `Cleve pre-1900` request labels as temporal evidence. The result set demonstrably includes post-1900 objects, including 1938 Preston Smith records. Promote individual rows only after checking object-level date and person identity.
4. **Sorbonne** — leave alone. The interrupted job produced a partial raw artifact but no complete normalised record layer. Revisit manually only for a specific Dejerine object/query later.

Everything else from this harvest pass is adequate for normalisation/export. These residuals do not reopen the frozen 307/155 census.
